import 'package:flutter/material.dart';

class SectionOnDelivery extends StatelessWidget {
  const SectionOnDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
