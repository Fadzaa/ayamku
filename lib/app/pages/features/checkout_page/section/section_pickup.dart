import 'package:flutter/material.dart';

class SectionPickUp extends StatelessWidget {
  const SectionPickUp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
